enum TypeMessage { audio, media, text ,like}
enum MessageStatus { notSent, sent, viewed }
enum TypeChat { normal, group }
enum RuleChat { member, admin }
enum TypeNotification { acceptFriend, addFriend, chat }
